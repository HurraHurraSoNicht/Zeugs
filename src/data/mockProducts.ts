import type { Product } from '../types/product';

// Emptied intentionally — the app now starts with no seed products; real
// entries are added via the Admin tab (Open Food Facts search or URL scrape).
export const mockProducts: Product[] = [];
