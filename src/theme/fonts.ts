// Font families loaded via expo-font in App.tsx (useFonts). These packages
// ship one static font file per weight rather than a single variable font,
// so — unlike system fonts — `fontWeight` alone does nothing; the desired
// weight must be selected by fontFamily name.
export const fonts = {
  heading: 'Fredoka_700Bold',
  body: 'Nunito_400Regular',
  bodyMedium: 'Nunito_600SemiBold',
  bodyBold: 'Nunito_700Bold',
};
